name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Verify font manifest
        run: node scripts/verify-font-manifest.js
      - name: Run rendering tests
        run: node test.js
